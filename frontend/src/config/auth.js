// Hardcoded credentials for the dashboard's built-in login gate.
// No backend, database, or token issuance is involved — this only guards
// client-side access to the dashboard UI. Change these values to update
// the login credentials.
export const AUTH_CREDENTIALS = {
  username: 'admin',
  password: 'admin123',
};
